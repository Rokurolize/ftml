| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Frozen references 20260815-11 and 20260815-12 | State the verified close-name behavior | Wikidot accepts `module654` as an opener but closes its body only with `[[/module]]` | Value | capture -> render -> compare -> fix -> regression | Wikidot module closer rule | The Wikidot module closer rule refers to the live behavior in which `module654` may open a module body but only `module` may close it. |
| Generated parity cases and bindings | Record the reviewed coverage population | `tests/fixtures/wikidot-parity/cases.jsonl` is the changing inventory denominator; preview-compatible rows must be accounted for exactly once by current-source references and `bindings.json` | Record | inventory -> capture -> binding -> snapshot | stable parity inventory | The stable parity inventory refers to the checked-in set of sources, classifications, references, bindings, and reviewed snapshots; its current counts are derived from the machine-readable artifacts rather than copied into this referent table. |
| Issue 503 and the pre-fix comparator verdicts | Preserve the functional failure and its disposition | FTML consumed CSS through an alias closer that live Wikidot rendered as page text | Event | mismatch -> issue -> root-cause fix -> matching verdict | confirmed parity mismatch | A confirmed parity mismatch refers to a comparator difference across DOM tree, DOM signature, and visible text that demonstrates an FTML behavior defect. |

| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| `conf/blocks.toml` and the stable parity inventory | Prevent configured block names from lacking frozen observations | Every configured block name and alias appears in at least one checked-in stable source | State | configure -> inventory -> capture -> compare | registered block-name coverage | Registered block-name coverage refers to the state in which every configured block name and alias appears in the stable parity inventory. |
| Configured star, score, and body fields | Prevent parser flag and close branches from lacking stable observations | Every block that accepts star or score has that marker in a stable source, and every block with a body has a stable closer source | State | configure -> enumerate facet -> inventory -> capture -> compare | configured block-facet coverage | Configured block-facet coverage refers to the state in which every configured star, score, and body-close branch appears in the stable parity inventory. |
| Configured aliases on blocks with bodies | Prevent cross-alias body ownership from being inferred from separate name cases | One configured opener and closer name pair appearing together in a stable source | Record | enumerate opener -> enumerate closer -> inventory pair -> capture -> compare | ordered alias-pair observation | An ordered alias-pair observation refers to one configured opener and closer name pair appearing together in a stable source. |
| Frozen reference batch 20260815-13 | Preserve discrepancies that FTML cannot resolve without caller state | The `embedvideo` provider result and `gallery` page/file selection result differ from direct FTML HTML rendering | Record | capture -> compare -> issue -> caller verification | caller-runtime mismatch | A caller-runtime mismatch refers to a frozen Wikidot comparison difference whose required resolution inputs belong outside FTML. |

| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Frozen Wikidot evidence and FTML security tests | Record a mismatch that must remain because matching Wikidot would weaken an enforced security property | A frozen output difference caused only by FTML rejecting or hardening unsafe Wikidot output | Value | identify functional failures, exclude impossible or unsafe byte matches, fix and test FTML-owned bugs | intentional-security-divergence | `intentional-security-divergence` refers to a frozen output difference that FTML deliberately preserves to enforce a security property. |
| FTML and Wikijump responsibility boundary | Record a mismatch that cannot be decided from syntax and page metadata alone | A frozen output difference whose result requires a site, page database, file service, permission, request, or browser state owned by the caller | Value | identify functional failures, exclude impossible or unsafe byte matches, fix and test FTML-owned bugs | caller-runtime | `caller-runtime` refers to a frozen output difference whose missing inputs belong to the caller runtime rather than FTML. |
| Three-surface comparison results | Record a mismatch that changes raw DOM text only and preserves structure and visible behavior | A frozen output difference where DOM signature and visible text match and no functional syntax failure is demonstrated | Value | identify functional failures, exclude impossible or unsafe byte matches, fix and test FTML-owned bugs | comparison-normalization | `comparison-normalization` refers to a frozen raw-DOM difference with no demonstrated functional behavior difference. |
